<!DOCTYPE html>
<html>
<head>
<style>

.container_container {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
  background-color: #5b6a5c;
}

.container_container > div {
  background-color: #f1f1f1;
  width: 100px;
  height: 50%;
  margin: 10px;
  text-align: center;
  line-height: 100%;
  font-size: 30px;
}

.container {
    position: relative;

    flex-grow: 1;
}

.image {
  opacity: 1;
  display: block;
  width: 100%;
  height: auto;
  transition: .5s ease;
  backface-visibility: hidden;
}

.middle {
  transition: .5s ease;
  opacity: 0;
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  -ms-transform: translate(-50%, -50%)
}

.container:hover .image {
  opacity: 0.3;
}

.container:hover .middle {
  opacity: 1;
}

.text {
  background-color: #4CAF50;
  color: white;
  font-size: 16px;
  padding: 16px 32px;
}
</style>
</head>
<body>

<h2>Fade in a Box</h2> <br> <br>
<div class="container_container">
  <% @dogs.each do |dog| %>
<div class="container">

  <img src="<%= dog.photos.first.medium %>" alt="Avatar" class="image" style="width:100%">
  <div class="middle">
    <div class="text">
      <%= link_to dog.name, dog_path(dog.id) %> <br>
      <hr>
      <%= dog.sex %> <br>
      <%= dog.age %>
    </div>
  </div>
</div>
<% end %>
</div>
</body>
</html>
