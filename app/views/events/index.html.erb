
  <% @events.each do |event| %>
<ul>
<%= link_to event.name, event %>
<li><%= event.address %></li>
<%= link_to "Delete", event, method: :delete, data: { confirm: "Really?" } %>

</ul>
<% end %>
</span>

<%= link_to "<button>Create event</button>".html_safe, new_event_url, :id => "button_id", :class => "btn btn-6 btn-6d" %> <br>
